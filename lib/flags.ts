export const FLAGS = {
  vipDay: true, // Controls VIP Day package visibility
  // Add other feature flags as needed
  // example: newFeature: false,
  // example: betaFeature: process.env.NODE_ENV === 'development',
};
